"use strict";

/*
Write a JavaScript function to check whether an `input` is an array or not.
*/

import { isArray } from "./isArray.js";

console.log(isArray("w3resource"));
console.log(isArray(125));
console.log(isArray([1, 2, 4, 0]));
